/**
 * Protocol barrel — the ONE importable home for the frozen Plexus M0 contract.
 *
 * This is the `@plexus/protocol` package: the shared keystone every other
 * package (runtime, cli, web-admin) imports as `@plexus/protocol`. It is the
 * single source of truth; `docs/protocol/types.ts` is a re-export mirror of it.
 */

export * from "./types.ts";
