{
  "name": "@plexus/web-admin",
  "version": "0.7.0-rc.1",
  "private": true,
  "type": "module",
  "description": "Plexus web admin — the React SPA management client, served same-origin by the runtime at /admin (and the future desktop renderer).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@plexus/protocol": "workspace:*",
    "@plexus/runtime": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.0",
    "vite": "^5.4.11"
  }
}
