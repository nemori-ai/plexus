/**
 * Protocol barrel — the ONE importable home for the frozen Plexus M0 contract.
 *
 * Source code across the gateway imports protocol types from here (or directly
 * from `./types.ts`). This is the single source of truth; `docs/protocol/types.ts`
 * is a re-export mirror of it.
 */

export * from "./types.ts";
