# Secret provisioning for `acme-notes`

Provision these secret VALUES into `~/.plexus/secrets/` (OS keychain where available).
The manifest carries only the REFERENCE; no value is ever written here.

- `acme-notes-api-key` — attached as `bearer`.
