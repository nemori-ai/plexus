/**
 * ============================================================================
 * PLEXUS — M0 PROTOCOL TYPES — SPEC MIRROR (NOT the source of truth)
 * ============================================================================
 *
 * FROZEN CONTRACT v0.1.0.
 *
 * The CANONICAL, importable definitions now live in the buildable source tree at:
 *
 *     src/protocol/types.ts
 *
 * This file is a thin RE-EXPORT MIRROR so the docs/ tree still type-checks and so
 * historical links to `docs/protocol/types.ts` keep resolving. There is exactly
 * ONE source of truth (src/protocol/types.ts); this file MUST NOT diverge from it.
 * Edit the canonical module, never this mirror.
 *
 * @see ../../src/protocol/types.ts  the canonical types (single source of truth)
 * @see ./PLEXUS-PROTOCOL.md         the human-readable contract + worked examples
 * @see ./DECISIONS.md               the ADRs behind each choice
 */

export * from "../../src/protocol/types.ts";
