# Tutorial screenshots

Drop the tutorial screenshots here. The docs under `docs/tutorials/` reference these
exact filenames:

| File | Referenced by | Shows |
| --- | --- | --- |
| `grant-approval.png` | [`connect-an-agent.md`](../../tutorials/connect-an-agent.md) | Approving a pending grant in the `/admin` **Pending** tab (with the trust-window picker). |
| `create-extension.png` | [`create-an-extension.md`](../../tutorials/create-an-extension.md) | The `/admin` **Create an extension** view — paste a manifest, preview its security surface, install. |

Capture both at `http://127.0.0.1:7077/admin` against a running gateway.
