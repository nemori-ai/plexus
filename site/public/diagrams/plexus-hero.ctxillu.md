# plexus-hero.png — contextual-illustrator sidecar

The landing-page hero concept plate. Part of the Plexus doc-site diagram series —
regenerate with the SAME house style for visual consistency.

- Model / backend: `gpt-image-2` via `fal`
- Aspect ratio: `16:9`
- Quality: high · Format: png
- Script: `bun ~/.claude/plugins/cache/vibe-skills/contextual-illustrator/1.0.0/skills/contextual-illustrator/scripts/generate_image.mjs`

## House style (verbatim — identical across the whole series)
Clean FLAT technical line-diagram, a self-contained figure on a warm dark charcoal background (deep warm near-black with a faint amber tint, like lamplight on a ledger). Draw the structure as LABELED rounded-rectangle nodes connected by thin directional arrows — this is a labeled flow/architecture diagram, NOT an icon illustration. Strokes: thin (~1.5px) warm off-white / cream. Use AMBER for the active/primary path and emphasis; a desaturated SAGE GREEN for allow/granted/exposed states; a muted CLAY RED for deny/revoke; muted SLATE grey for neutral/hidden. Crisp legible geometric sans-serif labels, every word spelled EXACTLY as specified. Generous whitespace, precise, mechanical, calm — a plate in a well-set technical manual. STRICTLY NO decorative device icons, NO neon, NO gradients, NO glow, NO 3D/isometric, NO drop shadows, NO photorealism, NO emoji, NO purple/cyan. Only labeled boxes, arrows, and text.

## Figure content (appended to house style)
FIGURE CONTENT — a left-to-right conceptual diagram in THREE zones, and NOTHING else:
(1) RIGHT zone, heading YOUR RESOURCES above FOUR small slate-grey rounded boxes of DELIBERATELY DIFFERENT widths (different shapes / different APIs), stacked, labeled top-to-bottom: notes, calendar, IoT, workspace. Thin arrows point LEFT toward the center panel; label the bundle 'describe'.
(2) CENTER zone, ONE tall AMBER-bordered panel titled PLEXUS with subtitle 'one self-describing capability object'. Inside: THREE identical uniform cream rows each labeled 'source.capability'. Down the LEFT edge, a thin vertical BOUNDARY line carrying two markers: an AMBER padlock labeled 'authorize / default-deny / revocable', and below it a SAGE GREEN ledger tick labeled 'audit / every call on record'.
(3) LEFT zone, a single cream node labeled AGENT. One amber arrow FROM panel boundary TO agent labeled 'read'; one amber arrow FROM agent INTO the boundary labeled 'list → invoke' (literal right-arrow glyph).
Lots of empty warm-charcoal space around everything. (Shipped image = v2_2: explicit boundary line with lock + tick on it.)
